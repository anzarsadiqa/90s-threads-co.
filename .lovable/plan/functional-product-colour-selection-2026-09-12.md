# Functional Product Colour Selection

- Make every product colour option a clear selectable control with an active state.
- When a product has one image per colour, selecting a colour will show its matching image; gallery selection will also keep the matching colour in sync.
- Preserve the selected colour in cart, checkout, order items, and admin order details using the existing flow.
- Reset size, colour, image, and quantity correctly when navigating directly between products.
- Verify the red/blue rugby shirt and another product can select colours and add separate colour variants to the cart.
